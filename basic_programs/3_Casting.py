# Casting should be performed only when necessary usually right before performing the operation

# Avoid unnecessary casting python is flexible with types, so casting isn't always needed

# Examples

x = 5
y = "5"
z = "23.43"

print(int(x))
print(int(y))
print(float(z))